{
  "name": "restaurant-finder",
  "version": "1.0.0",
  "description": "Restaurant Finder Web Application",
  "scripts": {
    "build": "cd frontend && npm run build",
    "dev": "cd frontend && npm run dev",
    "install": "cd frontend && npm install"
  },
  "dependencies": {},
  "engines": {
    "node": ">=18.0.0"
  }
} 